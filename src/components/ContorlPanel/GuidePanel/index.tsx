import styled from "styled-components";

const GuidePanel = () => {
  return <GuidePanelContainer></GuidePanelContainer>;
};

export default GuidePanel;

const GuidePanelContainer = styled.td`
  width: 50%;
  background-color: darkcyan;
`;
